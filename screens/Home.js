import { View, Text, StyleSheet } from "react-native";

const Home = () => {
	return (
		<View>
			<Text>Home Screen</Text>
		</View>
	);
};

const styles = StyleSheet.create({
	container: {},
});

export default Home;
